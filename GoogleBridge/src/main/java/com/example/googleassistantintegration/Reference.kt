package com.example.googleassistantintegration

class Reference {
    // Gotta read the shortcuts.xml for other apps
    // Looking for <capabilities> tag in the xml

    // There are built in intents like actions.intent.ORDER_MENU_ITEM that apps are supposed to usec
    // There are intent elements listed in <capabilities> that allow Google assistant to launch your stuff
    // These are generally meant to have a UI for the user to interact w/, so it shouldn't be as complex
}